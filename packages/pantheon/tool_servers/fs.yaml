command: harnx-fs-tools
description: Filesystem read/write access for agents
args:
  - --allow-repo-work
  - --allow-dev-tools

# To allow access to additional paths, shadow this file with your own copy at
# ~/.config/harnx/tool_servers/fs.yaml and add --allow-rwx /path or
# --allow-read /path in args.
