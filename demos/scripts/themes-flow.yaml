chunk_delay_ms: 35
turns:
  - text_chunks:
      - "Here is a simple example of a Rust function "
      - "that calculates the sum of an array:\n\n"
      - "```rust\n"
      - "// This function sums the elements of a slice\n"
      - "fn sum_array(arr: &[i32]) -> i32 {\n"
      - "    let mut sum = 0;\n"
      - "    for &item in arr {\n"
      - "        sum += item;\n"
      - "    }\n"
      - "    sum\n"
      - "}\n"
      - "```\n\n"
      - "It returns the sum as an `i32` value."
fallback_text: "(demo complete)"