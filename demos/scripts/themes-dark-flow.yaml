chunk_delay_ms: 35
turns:
  - text_chunks:
      - "Here is a richer Rust example demonstrating various syntax features:\n\n"
      - "```rust\n"
      - "// Basic setup\n"
      - "/// A magical configuration struct\n"
      - "#[derive(Debug, Clone)]\n"
      - "pub struct Config<'a> {\n"
      - "    name: &'a str,\n"
      - "    power: f64,\n"
      - "}\n"
      - "\n"
      - "impl<'a> Config<'a> {\n"
      - "    pub fn execute(&self, count: i32) -> Option<bool> {\n"
      - "        if count <= 0 { return None; }\n"
      - "        let msg = \"Start\\n\\tSequence\";\n"
      - "        let data = vec![1.0, 3.14, self.power];\n"
      - "        \n"
      - "        match self.name {\n"
      - "            \"wizard\" => Some(true),\n"
      - "            _ => {\n"
      - "                println!(\"Unknown {}: {}\", count, msg);\n"
      - "                Some(false)\n"
      - "            }\n"
      - "        }\n"
      - "    }\n"
      - "}\n"
      - "```\n\n"
      - "This covers macros, lifetimes, control flow, and literals."
fallback_text: "(demo complete)"
