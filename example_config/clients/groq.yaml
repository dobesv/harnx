# Groq Configuration (OpenAI-compatible)
# Env var: GROQ_API_KEY

type: openai-compatible
model_catalog: groq       # catalog block to inherit models from
api_base: https://api.groq.com/openai/v1
# api_key: "..."   # or set GROQ_API_KEY
