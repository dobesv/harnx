---
"coding": patch
---

chore(coding): update coder agent models to latest versions

Bump the `coder` agent default `model:` from `claude-sonnet-4-6` to
`claude-sonnet-5` and refresh its `model_fallbacks` (`gpt-5.4` → `gpt-5.6-sol`),
and move the `compact-coder` compaction agent from `gemini-3.1-flash-lite` to
the GA `gemini-3.5-flash-lite`.
