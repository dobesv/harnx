---
harnx: minor
---
Add ACP (Agent Client Protocol) support - both as a client connecting to ACP server sub-agents and as a server via `--acp <agent>` flag. Includes config support for `acp_servers`, ACP tool dispatch, and stdio transport.
